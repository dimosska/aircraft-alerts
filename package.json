{
  "name": "aircraft-alerts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js",
    "test": "node --test",
    "test:push": "node --env-file=.env scripts/push-smoke.js"
  }
}
